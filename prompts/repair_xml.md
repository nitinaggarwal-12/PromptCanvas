The draw.io XML below failed automated layout validation. Fix ONLY the listed errors. Do not add, remove, or rename any nodes or edges. Do not change styles. Only adjust mxGeometry x, y, width, height values.

ERRORS
{validation_report_json}

FIX RULES
- OVERLAP: move the second-listed cell to the nearest free position at least 60px right or 110px below.
- OUT_OF_CONTAINER: move the child inside the parent bounds, or grow the parent's width/height to enclose it with 30px padding.
- GEOMETRY_MISSING: assign width=180 height=72 at a free position inside the correct parent.
- After fixing, re-check that your fixes did not create new overlaps.

XML
{current_xml}

Output only the corrected XML. No markdown fences.
