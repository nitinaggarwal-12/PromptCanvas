You maintain an existing architecture graph (JSON below). Apply the user's requested change with the MINIMUM diff: only add/remove/modify the nodes, edges, or tiers required. Keep all other ids, labels, and fields byte-identical. Continue sequential id numbering from the current maximum. Output ONLY the complete updated JSON.

CURRENT GRAPH
{current_graph_json}

USER CHANGE REQUEST
{user_prompt}
